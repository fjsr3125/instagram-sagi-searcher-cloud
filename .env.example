# ===========================================
# Instagram 詐欺チェッカー Cloud版 - 環境設定
# ===========================================
# このファイルをコピーして .env を作成してください
# cp .env.example .env

# ===========================================
# Instagram認証情報（必須）
# ===========================================
# チェック用のInstagramアカウント情報
# 注意: メインアカウントではなくサブアカウントを推奨
#
# 複数アカウント形式（推奨）:
# 1時間30フォロー、1日60フォロー上限のため、複数アカウントでローテーション
# JSON配列形式で記述
INSTAGRAM_ACCOUNTS='[
  {"username": "account1", "password": "password1"},
  {"username": "account2", "password": "password2"},
  {"username": "account3", "password": "password3"}
]'

# 単一アカウント形式（後方互換性のため残存）:
# INSTAGRAM_ACCOUNTSが未設定の場合にのみ使用
# INSTAGRAM_USERNAME=your_username
# INSTAGRAM_PASSWORD=your_password

# ===========================================
# Redroid (Android Emulator) Settings
# ===========================================
REDROID_ADB_PORT=5555
REDROID_WIDTH=1080
REDROID_HEIGHT=1920
REDROID_DPI=480
REDROID_FPS=30
# GPU Mode: guest (クラウド環境ではguestを使用)
REDROID_GPU_MODE=guest

# ===========================================
# Appium Server Settings
# ===========================================
APPIUM_PORT=4723

# ===========================================
# scrcpy-web (Screen Viewer) Settings
# ===========================================
SCRCPY_WEB_PORT=6080

# ===========================================
# Web UI (FastAPI) Settings
# ===========================================
WEB_UI_PORT=8000
DEBUG=false

# ===========================================
# HTTPS Settings (Optional)
# ===========================================
# DuckDNSで取得したドメインを設定
# 例: DOMAIN=insta-checker.duckdns.org
# 設定後、docker compose --profile https up -d で起動
DOMAIN=

# ===========================================
# Checker Settings
# ===========================================
# アカウント間の待機時間（秒）
CHECK_DELAY=10
# バッチサイズ（この件数ごとにセッション再起動）
BATCH_SIZE=20
